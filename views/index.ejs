<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %> list: </h1>
    <p>Welcome to <%= title %> :</p>
    <ul>
      <% for (var i = 0; i < data.length; i++) {%>
      <li><%= data[i].id + data[i].names + data[i].no_seats %></li>

      <% } %>
    </ul>
  </body>
</html>
